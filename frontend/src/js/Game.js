function dice(userDice){
    userDice = Math.random()*6+1;
}
function randomDrow(randomNum,drowNum){
    drowNum=Math.random()*randomNum+1;
}
function vote(userNum,voteList){
for(let i =0;i<userNum;i++){
    voteList[i]
}
}
export{
    dice,
    randomDrow,
    vote,
}